package shapes;
// How do I import Input class here?
import util.Input;

public class CircleApp {

    public static void main(String[] args) {
        // SOUT comes from Input Class


        double userInput;
        // How can I get the PRIVATE scanner from Class Input into here?
        // Instruction:
        /*Inside of shapes, create a class named CircleApp prompting the user for the radius of the circle using your Input class, creates a Circle object, and displays the circumference and area. (Note you will need to import your Input class.)
        * Formula for circumference:
        *       circumference = 2 * pi * radius
        *       area = pi * (radius ^ 2)
        * For the value of pi, use the PI constant of the Math class.*/
    }
}
